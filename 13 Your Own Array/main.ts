//  assignment no 13
//  Your Own Array
let transportatiomMode: string[] = ["plane","train","car","bus"]
for (let i = 0; i < transportatiomMode.length; i++) {
    console.log(`i would like to own a ${transportatiomMode[i]}`);
    
}