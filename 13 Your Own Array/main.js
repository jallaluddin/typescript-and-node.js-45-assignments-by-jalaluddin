//  assignment no 13
//  Your Own Array
var transportatiomMode = ["plane", "train", "car", "bus"];
for (var i = 0; i < transportatiomMode.length; i++) {
    console.log("i would like to own a ".concat(transportatiomMode[i]));
}
